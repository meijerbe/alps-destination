{
  "name": "ab-op-reis-tests",
  "private": true,
  "description": "Browsertests voor A&B op reis. Staat bewust apart van de site zelf, zodat de root geen package.json heeft en Vercel index.html gewoon statisch blijft serveren.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "lint": "cd .. && tests/node_modules/.bin/eslint -c eslint.config.mjs js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "eslint": "^10.1.0"
  }
}
